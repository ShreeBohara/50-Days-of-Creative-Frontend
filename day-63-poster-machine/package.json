{
  "name": "day-63-poster-machine",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "for f in js/*.js js/systems/*.js; do node --check \"$f\" || exit 1; done"
  }
}
