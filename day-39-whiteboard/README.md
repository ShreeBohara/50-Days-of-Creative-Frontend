# Day 39 - Real-Time Collaborative Whiteboard

A mini Excalidraw-style infinite canvas built with React, Zustand, Canvas API, and BroadcastChannel.

## Features

- Infinite canvas with pan, zoom, minimap, and optional dot grid.
- Freehand drawing, lines, rectangles, ellipses, arrows, text, sticky notes, eraser, selection, resizing, and layers.
- Undo/redo, keyboard shortcuts, PNG export, JSON save/load, and same-browser multi-tab sync.

## Shortcuts

- `1-9` switches tools.
- `Delete` removes the current selection.
- `Ctrl/Cmd+Z` and `Ctrl/Cmd+Shift+Z` undo and redo.
- `+`, `-`, and `0` control zoom.

## Scripts

```bash
npm run dev
npm run lint
npm run test
npm run build
```

Built output is configured for GitHub Pages at `/50-Days-of-Creative-Frontend/day-39-whiteboard/`.
