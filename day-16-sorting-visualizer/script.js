'use strict';

/* Project scaffold. Interactive behavior lands in later commits. */
const root = document.getElementById('app-root');

if (root) {
  root.textContent = 'Sorting visualizer loading...';
}
